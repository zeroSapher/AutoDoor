# Proteus 仿真方案（AutoDoor）

> 目标：在 Proteus 中**分模块验证**，而不是硬啃整个闭环系统。
> Proteus 对执行机构、传感器的建模是理想化的，而且**它没有可信的 SG90 舵机模型**，
> 试图仿真整机会消耗大量时间却得不到可信结论。

---

## 1. 先说清楚：仿真能验证什么、不能验证什么

这是本文档最重要的一节。仿真与真机的能力边界不同，**把仿真结论当成真机结论是危险的**。

| 项目 | 仿真能否验证 | 说明 |
|---|---|---|
| 门体状态机全部转移 | ✅ **能，且这是主要价值** | 与真机完全同一份 `Door.c` |
| 串口命令协议 | ✅ 能 | Virtual Terminal 直接收发 |
| **舵机 PWM 波形（50 Hz 帧与脉宽）** | ✅ **能，且这是执行机构一侧唯一可验的部分** | 虚拟示波器/计数器看 `PA6`：帧周期 20 ms，脉宽在 **1000 ↔ 2000 µs** 之间被逐帧扫过（见 §4.3） |
| 位置推算（`Limit_IsOpen/IsClosed` 的语义） | ✅ 能 | 与真机默认构建相同：按**舵机运动方向 + 已走时间**推算，约 1.5 s 到位（默认 `DOOR_TRAVEL_MS`） |
| 按键消抖与长短按 | ✅ 能 | 按键元件 |
| **OLED 显示** | ✅ **能** | 使用 Proteus 的 **OLED12864 I2C**（SSD1306-compatible）模型 |
| **EEPROM 持久化** | ❌ **不能** | 仿真固件使用 **EEPROM stub**：它给每次传输都回 NACK，所以仿真里**什么都不落盘** —— 日志走降级路径，`DELAY=` / `MODE=` / `TRAVEL=` 每次上电都回 `(NOT PERSISTED - eeprom write failed)`，`boot#` 与记录数也从不保持 |
| **舵机真的转过 90°** | ❌ **不能** | **Proteus 没有可信的 SG90 模型**，机械角度无法仿真 —— 仿真里能看到的是 `PA6` 上的脉冲，不是门体转了多少度。真实 90° 只能在真机上量 |
| **限位开关这条链**（EXTI 立即停机、限位极性、`LIMIT_CONFLICT`） | ❌ **不能** | 仿真固件**也不装限位**（它继承默认构建），没有开关可以拨。要验只能用 `-WithLimits` 另做一套真机变体 |
| **硬件限位断电回路** | ❌ 不能 | 该方案已作废（本设计不装限位），见 `docs/接线图.md` 第 5 节 |
| **中断是否真的触发** | ⚠️ 不确定 | 见下方"EXTI 风险" |

### ⚠️ EXTI 风险与我们的应对

Proteus 的 STM32 模型对 NVIC/EXTI 的支持历来看法不一，部分版本上外部中断不稳定。

**好消息是：本固件不依赖中断边沿也能工作。** `Debounce_Tick1ms()` **每毫秒主动采样引脚电平**，
中断只用来**提前启动稳定窗口**（降低延迟），不参与正确性判断。

这是本轮专门为此加固的：原实现只在中断置位后才检查电平，一旦漏掉边沿，
输入就会永久卡在旧电平。现在即使 **EXTI 完全不触发**，按键与传感器也能正常识别，
只是响应慢 20–25 ms（消抖窗口长度）。

> 这一改动同时在真机上消除了"漏边沿导致输入冻结"的隐患，不只是为了仿真。
> 注意它**不再涉及限位**：本设计不装限位开关，仿真与真机默认构建都不配置 EXTI0/EXTI1。

---

## 2. 仿真固件的编译

```powershell
.\tools\build-sim.ps1 -Clean
```

产物：`build-sim/AutoDoorSim.hex` → 在 Proteus 的 STM32 模型属性里设为 **Program File**。

**与真机固件的差异**（仅两处，业务代码完全相同）：

| | 真机 `build/AutoDoor.hex` | 仿真 `build-sim/AutoDoorSim.hex` |
|---|---|---|
| 显示后端 | `Display_Oled.c` + SSD1306 | `Display_Oled.c` + OLED12864 I2C |
| 存储后端 | `EEPROM.c` + AT24C32 | `EEPROM_Stub.c`（报告器件缺失） |
| Flash 占用 | 22692 B (34.6%) | 19020 B (29.0%) |

两者共用 `tools/sources.ps1` 的源文件清单，不可能各自漂移。

> **限位在两边都不存在**：`build-sim.ps1` 不传 `-WithLimits`，所以仿真固件与真机默认构建
> 一样是 `AUTODOOR_NO_LIMITS=1`（位置按行程时间推算）。仿真里**没有限位开关可以拨**，
> 这是刻意的 —— 它验证的是与真机同一份逻辑，而不是一个只有仿真才有的模式。

---

## 3. 仿真原理图接线

### 3.1 MCU 与基础

| 项 | 接法 |
|---|---|
| U1 | STM32F103C8（Proteus 库中搜 `STM32F103C8`） |
| 晶振 | **建议直接用模型的内部 8 MHz HSI**，与真机一致 —— 这块真机的 HSE 晶振从未起振（HSERDY 即使强制 HSEON 也不置位），固件就是跑 HSI 的。若一定要画 8 MHz + 22 pF ×2 接 OSC_IN/OSC_OUT，见 §5 的时钟注意：**两种情况下 TIM3 的 20 ms 帧与 1 ms SysTick 都应当准确**（固件按真实 APB1 定时器时钟推导预分频） |
| 复位 | NRST 经 10 kΩ 上拉，接一个按键到 GND |
| BOOT0 | 经 10 kΩ 下拉到 GND |
| 电源 | VDD/VDDA 接 +3.3V，VSS/VSSA 接 GND，每个 VDD 配 100 nF |

### 3.2 OLED12864 I2C 显示

| OLED12864 引脚 | 接 MCU |
|---|---|
| SCL | **PB10** |
| SDA | **PB11** |
| VCC | **+3.3 V** |
| GND | **GND** |

**还要加两个上拉电阻**：`PB10` 和 `PB11` 各经一个 **4.7 kΩ（或 10 kΩ）到 +3.3 V**。

> ⚠️ **这一步漏掉的话，开机第一行就会是 `I2C idle : NO (bus held low)`，`Display : not found`。**
>
> I2C 是开漏总线，**没有上拉就没有高电平**。固件的 `MyI2C_IsIdle()` 一上电就检查两条线
> 是否都被释放到高；靠 10 kΩ 上拉（见 §3.3）读不到"H"时，它会如实报告总线被拉死 ——
> 那是**正确的诊断**，不是 bug。真机上 OLED 模块通常自带这两个电阻，所以这一点只在
> 仿真里容易漏。

> 仿真使用 OLED12864 的 I2C 接口，因此 PB10/PB11 与真机保持一致；
> EEPROM 仍由 `EEPROM_Stub.c` 替代，仿真**不会**验证持久化（见 §4.1）。

### 3.3 输入（按键 / 拨码开关）

| 网络 | 引脚 | 元件 | 说明 |
|---|---|---|---|
| **PA0 / PA1** | — | **不接任何元件** | 限位已取消：仿真固件与真机默认构建都不读这两个引脚 |
| 门外（进门） | **PB12** | 按键到 GND + 10 kΩ 上拉 | 按下 = 低电平 = 检测到人 |
| 门内（出门） | **PB13** | 同上 | |
| KEY1 启停 | **PB5** | 按键到 GND + 10 kΩ 上拉 | |
| KEY2 模式 | **PB6** | 同上 | |
| KEY3 手动开门 | **PB7** | 同上 | |
| KEY4 手动关门/急停 | **PB8** | 同上 | 长按 = 急停 |

> **没有限位开关要接**：门的位置由 `Limit.c` 按**舵机运动方向 + 已走时间**推算
> （0 % 全关 → 100 % 全开，全程 `DOOR_TRAVEL_MS` 默认 1500 ms，可用 `TRAVEL=<ms>`
> 在 600..4000 内现场改 —— 仿真里它同样只能**本次运行**生效，见 §4.1）。
> 所以仿真里"到位"发生在**发命令后约 1.5 s**，不需要（也没有）任何开关去拨。
> 想看限位那套逻辑（低有效读回、`LIMIT_CONFLICT`、EXTI 立即停机），
> 只能另做 `-WithLimits` + 真机接线的一套，见 `docs/接线图.md` §2.2。

### 3.4 舵机驱动（**只能验波形，不能验机械**）

**Proteus 没有可信的 SG90 模型**，所以这里**不摆舵机模型**，也不假装有。
能做的只有诚实的一件事：**量 `PA6` 上的驱动脉冲**。

| 项 | 接法 |
|---|---|
| 信号 | `PA6`（TIM3_CH1）接 **OSCILLOSCOPE** 通道（或计数器） |
| 观察量 | **帧周期 20 ms（50 Hz）**；**高电平脉宽**在 1000 µs（全关）与 2000 µs（全开）之间 |
| 要不要接负载 | **不需要**。舵机的机械 90° 在本仿真里**无法验证** |
| 可选 | 若只想看"脉冲有没有被推动"，可以给 `PA6` 挂一个 `COUNTER` 或逻辑分析仪通道，不必接任何执行器 |

> **不要**用一个"看起来像舵机"的元件去代替它：Proteus 器件库里没有可信的 SG90，
> 随便找个 `SERVO` 或 `MOTOR` 元件接上，得到的只是那个元件自己的行为，
> 与真机 SG90 无关 —— 那比不接更危险，因为它会给出一个**看起来通过**的结论。
>
> **这里能证明的**：固件确实在 `PA6` 上产生了 50 Hz、脉宽正确、并按行程时间逐帧推进的脉冲。
> **这里证明不了的**：舵机是否真的转到位、门的机械 90° 是否准确、堵转与电流。
> 这三件事只能上真机（见 §7）。

### 3.5 蜂鸣器与 LED

| 网络 | 引脚 | 元件 |
|---|---|---|
| 蜂鸣器 | **PB3** | `BUZZER`（有源）或 `SOUNDER` |
| 状态 LED | **PB4** | LED + 330 Ω 到 GND |

### 3.6 串口

| 项 | 接法 |
|---|---|
| USART1 | PA9(TX) → `COMPIM`/`VIRTUAL TERMINAL` 的 RXD；PA10(RX) ← TXD |
| 参数 | **115200 8N1** |

---

## 4. 五个分模块测试

按顺序做，每步都有**明确的判定标准**。

### 4.1 测试一：启动与自检

**运行仿真**，Virtual Terminal 应输出：

```
========================================
 AutoDoor  STM32F103C8T6  v1.0
========================================
I2C idle      : yes                    ← 预期：OLED12864 I2C 总线空闲
Display       : OK                     ← 预期：OLED12864 应答
EEPROM @0xA0  : FAILED at 0x0000       ← 预期：stub 报告器件缺失
Log           : UNAVAILABLE - running with defaults
*** NO LIMIT SWITCHES: position is a timed estimate (1500ms) ***
*** the travel watchdog (2700ms) is the only overrun protection ***

Position      : timed estimate, 1500ms travel (open=0 closed=1)
    state: INIT -> IDLE
Door          : IDLE (press KEY1 to enable)
```

> 顺序按上面这份对：`I2C idle` / `Display` / `EEPROM` / `Log` 四行在最前，
> 两条 `***` 警告与 `Position` 在其后（打印的是**当前生效**的行程时间）；
> 缩进的 `    state: INIT -> IDLE` 是状态回调自己的一行，不是串口把两行接在了一起。

**判定**：
- ✅ `Log: UNAVAILABLE` 出现 → **降级路径正确**（这是仿真特意验证的行为）
- ✅ `Door: IDLE` → 上电时推算位置为 0（全关），位置判断正确
- ✅ 那两行 `*** NO LIMIT SWITCHES … ***` → **正常**：仿真与真机默认构建都不装限位，
  这是出货配置的自我说明，不是仿真特有的警告
- OLED12864 第一行显示 `AutoDoor`，其余区域显示状态和事件

> 仿真**不能**验证 `EEPROM: OK` 与 `records=N` 持久化，那需要真机。
> stub 对每一次传输都回 NACK，所以仿真里**没有任何东西是持久的**：`boot#` 与
> `records=` 不会保持，`DELAY=<ms>` / `MODE=` / `TRAVEL=<ms>` 每次都会在正常回显后
> 追加 ` (NOT PERSISTED - eeprom write failed)`（`DEFAULTS` 同理），下一次上电又回到
> `delay=5000ms` / `mode=AUTO` / `travel=1500ms`。**这些值是"本次运行"的，不是"这台设备"的** ——
> 想验证"断电后还在不在"，只能上真机。

> **一个副作用要提前说明**（round 10 起）：没有 EEPROM 时 `LOG?` / `LOG?ALL` / `LOG?<n>`
> 会回 `ERR EEPROM (log not fully persisted)`，而不是以前那句 `OK LOG EMPTY`。
> 这是**故意的**：日志不可用时回"空"是在撒谎 —— 它根本无从知道有没有记录。
> （`LOG?CLEAR` 本来就回 `ERR EEPROM`，没有变化。）
> 仿真里请把这些 `ERR EEPROM` 当作**预期输出**，它和 `Log: UNAVAILABLE`
> 一起证明降级路径是自洽的。

### 4.2 测试二：按键与模式

| 操作 | 预期 |
|---|---|
| 按 KEY1 | 串口出现 `SYSTEM_START`，状态从 `STOPPED`/`IDLE` 变化；OLED 状态变化 |
| 按 KEY2 | `MODE_CHANGE`，模式在 AUTO/MANUAL 间切换 |
| 按 KEY1 再按 | `SYSTEM_STOP` |

**判定**：每次按键**只产生一条**事件。若一次按下出现多条 → 消抖窗口过短。

### 4.3 测试三：舵机脉冲波形（虚拟示波器 / 计数器）

1. 给 **`PA6`** 接一个 **OSCILLOSCOPE** 通道（**不要接任何舵机模型**，见 §3.4）
2. 按 KEY1 使能，按 **KEY3**（手动开门）
3. 观察波形

**预期**：

- **帧周期约 20 ms（50 Hz）**，全程稳定不变 —— 舵机要的就是稳定的 50 Hz 帧；
- 开门时**高电平脉宽从约 1000 µs 朝 2000 µs 推进**，默认
  `DOOR_TRAVEL_MS = 1500 ms` 走完这段；关门（KEY4）时反向；
- 脉宽是**逐帧变化**的：1500 ms 里每 20 ms 一帧，每帧约走 **13 µs**（推导见设计文档 3.3）；
- **没有换向死区、也没有占空比**：全程只有一路脉冲，这两样东西在本分支不存在。

**判定**：

- ❌ 帧周期不是约 20 ms（或不断抖动） → TIM3 的分频/重载值不对；
- ❌ 脉宽一步跳到 2000 µs → 行程推进没生效，真机上舵机会"啪"地跳到终点；
- ❌ 脉宽超出 1000–2000 µs 范围 → 会顶到舵机的机械极限，真机上表现为堵转发热。

> **这是仿真在舵机这一侧唯一有价值的一项，而它的价值是有限的**：
> 它只证明"脉冲对"，**不证明"舵机真的转过了那 90°"** —— Proteus 没有可信的 SG90 模型，
> 机械部分无法仿真。所以这一项通过之后，机械那一半仍然必须上真机（见 §7）。

### 4.4 测试四：门体状态机全流程

用按键模拟传感器。**没有限位开关要拨**：位置按行程时间推算，默认约 1.5 s 到位。

| 步骤 | 操作 | 预期串口 |
|---|---|---|
| 1 | KEY1 使能 | `SYSTEM_START` |
| 2 | 按"门外"键 | `ENTER_IN` → `OPEN_START` → OLED 显示 `OPENING` |
| 3 | **什么都不按，等约 1.5 s** | `LIMIT open edge` → `OPEN_DONE DUR=xxxxms` → `OPEN`（状态**自己**变，没有开关可拨） |
| 4 | 等待 5 s 不操作 | `CLOSE_START` → `CLOSING`，蜂鸣器响 |
| 5 | **再等约 1.5 s** | `CLOSE_DONE` → `IDLE` |
| 6 | 重复 2，然后立刻再按一次"门外" | 倒计时**重新开始**（门保持开启） |
| 7 | 关门途中按"门内"键 | `REVERSE` → 门立即反转到 `OPEN` |

> 第 7 步是**最重要的安全行为**：关门途中检出人员必须反转。
>
> 第 3 / 5 步里的 `LIMIT open edge` / `LIMIT close edge` 不是开关产生的边沿，
> 而是**推算到位时固件自己产生的那条边沿**（`Limit.c` 的 `s_simOpenEvent`）。
> 串口看到它只说明"固件认为到了"，不代表有一个限位开关被压下。

### 4.5 测试五：故障与急停

| 注入 | 预期 |
|---|---|
| 在门仍处于 `OPENING`/`CLOSING` 时，用 SWD 把行程起点推早：`set var 'Door.c'::s_travelStartMs = g_msTick - 4000` | 超过 `DOOR_TRAVEL_TIMEOUT_MS`（默认构建 **2700 ms** = 1500 + 1200 余量）→ `FAULT_OPEN_TIMEOUT` → `STOPPED`，蜂鸣器三短鸣，LED 双闪 |
| 长按 KEY4 | 立即停机 + `ESTOP`，长鸣 |
| 急停后再发 `DOOR=OPEN` | **不动作**（故障锁存） |
| 用串口发 `RESET` | 故障清除，可以重新使能 |
| 同时在串口发 `STATUS?` | 显示 `FAULT=ESTOP`，并带 `LIMITS=TIMED` |

> **"堵住门 → 超时"这条在仿真和真机默认构建里都不成立**：位置是推算值，
> 舵机只要还在被命令扫向目标脉宽，推算位置就照样在约 1.5 s 后到达端点并判"到位"。
> 看门狗只有在状态机**没有收敛**时才报 —— 这也是"没有位置反馈"的代价之一
> （见 `Core/main.h` 的 "WHAT IS GIVEN UP"）。

---

## 5. 常见问题

| 现象 | 原因与处理 |
|---|---|
| OLED 只显示空白 | 检查 OLED12864 的 VCC/GND、PB10/PB11、地址和 I2C 上拉 |
| OLED 无显示 | 确认使用 SSD1306-compatible OLED12864 I2C 模型，地址为 `0x78` |
| 门外/门内按键无反应 | 确认接的是 **PB12/PB13**（不是 PB0/PB1——那是旧引脚） |
| 串口无输出 | 检查 PA9→RXD 是否**交叉**，波特率是否 115200 |
| 按键无反应 | 若 EXTI 在你的 Proteus 版本上不触发，**等 20–25 ms** —— 固件靠 1 ms 采样兜底，应当仍能识别 |
| 找不到"限位到位"的动作 | **正常**：本设计不装限位，仿真里也没有开关可拨。到位发生在发命令后约 1.5 s（默认 `DOOR_TRAVEL_MS` = 1500 ms，`TRAVEL=<ms>` 可改，但仿真里改了不持久化），串口的 `LIMIT open edge` 是推算到位时固件自己产生的边沿 |
| `STATUS?` 里没有 `LIMITS=TIMED` | 你装的是 `-WithLimits` 变体（它读 PA0/PA1）。仿真请用 `tools/build-sim.ps1` 的镜像，那是默认构建 |
| 舵机不动 | **仿真里正常**：Proteus 没有可信的 SG90 模型，本仿真**不接执行器**，只看 `PA6` 的脉冲波形即可（§3.4、§4.3） |
| 时钟不对导致延时错乱 | 仿真与真机都跑**内部 8 MHz HSI**（这块板的 HSE 晶振从未起振）。若在模型属性里指定了外部晶振频率，请确认它与固件实际使用的时钟一致 —— 否则 20 ms 帧与 1 ms SysTick 都会偏 |

---

## 6. 仿真交付物清单

| 文件 | 说明 |
|---|---|
| `build-sim/AutoDoorSim.hex` | 装入 STM32 模型的程序文件 |
| `build-sim/AutoDoorSim.elf` | 带调试信息，可用于单步 |
| 本文档 §3 的接线表 | 照着连即可 |
| 本文档 §4 的测试步骤 | 5 项分模块验证 |

> Proteus 工程文件（`.pdsprj`）需在 Proteus 内绘制 —— 我无法生成或验证其内部格式，
> 但上面的接线表与测试步骤是**最小无歧义集**。

---

## 7. 必须转到真机验证的项目

仿真通过**不等于**系统可用。以下项目只能在真机验证，见 `docs/上电调试步骤.md`：

1. **EEPROM 持久化**（断电重启后记录与配置是否保持，含 `TRAVEL=<ms>` 设的行程时间）
2. **行程时间标定 `DOOR_TRAVEL_MS`**（真门实测，且**宁长勿短**）——
   这是无限位方案里唯一的位置来源
3. **I2C 总线在舵机噪声下的健壮性**（`BusRecover()` 是否会真的被触发）
4. **舵机真的转过 90°**：连杆装好后门体的实际摆幅、以及**舵机是否需要独立 5 V（≥1 A）**
   —— 仿真里**无法验证机械角度**，而电源不足会表现为"门动到一半 MCU 复位"（见 `docs/接线图.md`）
5. **蜂鸣器与 LED 的实际可辨识度**
6. **OLED 显示**（仿真中从未跑过 OLED 后端）
7. **限位这条链**（`-WithLimits` 变体专用）：需要真的装上限位开关，
   才能验限位极性、`LIMIT_CONFLICT` 与"限位触发立即停机"。
   ~~硬件限位断电回路（NC 串 `+5V → VM`）~~ 已作废：本设计不装限位，
   那条回路的死锁问题见 `docs/接线图.md` 第 5 节（已标明作废）
