# AutoDoor 接线图（按模块拼装，非 PCB）

> **这份文档的引脚表来自 `generated/netlist.txt`，那是 `tools/gen-hardware.py` 从
> `Core/main.h` 解析出来的。** 也就是说：**图与固件不会不一致** —— 这是本项目
> 一开始就建立的机制（`main.h` 是机器可读的唯一真相，工具负责交叉校验）。
> 如果你改了 `main.h`，重跑 `python tools/gen-hardware.py` 再对照本表。

---

## 0. 先选你要接到哪一步

| 目标 | 需要接的 | 备注 |
|---|---|---|
| **A. 只确认能烧、能连** | **什么都不接**，只插 ST-Link | `docs/上电调试步骤.md` 步骤 0 |
| **B. 台架：跑状态机/串口/日志/OLED** | + USB-TTL、OLED、EEPROM、4 个按键、2 个传感器按键、蜂鸣器、LED | **不接限位和电机**，固件用 `-NoLimits` 构建 |
| **C. 完整（含电机与限位）** | + 5 V 电源、L9110S、电机、2 个限位开关 | **接线前先读第 5 节** |

> B 阶段就足以验证 90% 的固件逻辑（状态机全流程、12 条串口指令、日志持久化、
> OLED 三屏）。**建议先走完 B 再碰电机**，否则一旦有问题你分不清是代码还是接线。

---

## 1. MCU 引脚总表

### 1.1 需要接的

| MCU 引脚 | 接到 | 方向/电平 | 备注 |
|---|---|---|---|
| **PA0** | 开门限位 · `NO` 触点 | 输入，**低 = 到位** | 内部上拉已开 |
| **PA1** | 关门限位 · `NO` 触点 | 输入，**低 = 到位** | 内部上拉已开 |
| **PA4** | L9110S **A-1A** | 输出（软件 PWM） | 100 Hz 载波，1% 分辨率 |
| **PA5** | L9110S **A-1B** | 输出（方向） | 与 A-1A 组合出正/反/刹/停 |
| **PA9** | USB-TTL 的 **RXD** | 输出 | 115200 8N1 |
| **PA10** | USB-TTL 的 **TXD** | 输入 | **TX/RX 必须交叉** |
| **PA13** | ST-Link **SWDIO** | — | 必须保留 |
| **PA14** | ST-Link **SWCLK** | — | 必须保留 |
| **PB3** | 蜂鸣器信号（或 NPN 基极经 1 kΩ） | 输出，**高 = 响** | JTAG 由固件自动关闭 |
| **PB4** | 状态 LED（串 **330 Ω** 到 GND） | 输出，**高 = 亮** | JTAG 由固件自动关闭 |
| **PB5** | KEY1 → GND | 输入，低有效 | 启停系统 |
| **PB6** | KEY2 → GND | 输入，低有效 | 切换 AUTO/MANUAL |
| **PB7** | KEY3 → GND | 输入，低有效 | 手动开门 / 日志上翻 |
| **PB8** | KEY4 → GND | 输入，低有效 | 手动关门 / **长按 1.5 s = 急停** |
| **PB10** | OLED `SCL` + EEPROM `SCL` | 开漏 | **各需 4.7 kΩ 上拉到 3V3** |
| **PB11** | OLED `SDA` + EEPROM `SDA` | 开漏 | **各需 4.7 kΩ 上拉到 3V3** |
| **PB12** | 门外（进门）传感器 → GND | 输入，低有效 | 现在是按键 |
| **PB13** | 门内（出门）传感器 → GND | 输入，低有效 | 现在是按键 |
| **3V3** | OLED VCC、EEPROM VCC、两路上拉 | 电源 | 由板上 AMS1117 提供 |
| **5V** | 见第 3 节 | 电源 | **不要用 ST-Link 的 5 V 带电机** |
| **GND** | 所有模块 GND、电源负极 | — | **单点汇聚，见 3.2** |

### 1.2 明确**不要**接的

| 引脚 | 为什么 |
|---|---|
| **PC13** | `main.h` 里有 `LED_*` 宏指向它，那是**重写 `main.c` 时留下的死宏，固件从不驱动**。状态 LED 在 **PB4**。接 PC13 会得到一个永远不亮的灯 |
| PA2 / PA3 | USART2，留给 HC-05，本阶段不用 |
| PA6 / PA7 / PA8 / PA15 | **仅仿真**（Proteus 的 1602 曾用；现在空着） |
| PB0 / PB1 / PB2 / PB9 / PB14 / PB15 | 未使用 |

> **输入引脚全部启用了 MCU 内部上拉（`GPIO_Mode_IPU`）**，所以标注"→ GND"的按键
> **不接外部上拉也能工作**。加 10 kΩ 外部上拉 + 100 nF 到地只是提高抗噪性 ——
> 电机在旁边转的时候，这个建议值得听。

---

## 2. 各模块怎么接

### 2.1 L9110S + 130 电机

L9110S 双路模块只用 **A 路**（固件只用一相）：

| L9110S 模块 | 接到 |
|---|---|
| `A-1A`（有的板子印 `IA`） | **PA4** |
| `A-1B`（印 `IB`） | **PA5** |
| `VCC` | **+5 V**（见第 5 节 —— 这里有个坑） |
| `GND` | 电源 GND |
| `MOTOR A` 两个端子 | 130 电机两根线（**不分正负**，转向反了就对调） |
| `B-1A` / `B-1B` / `MOTOR B` | 空着不用 |

**电机端子必须并一个 SS34 续流二极管**：阴极接 **+5 V**（电机正极侧），阳极接另一端。
130 电机是感性负载，换向和 PWM 关断时会产生反峰，没有续流二极管会打 L9110S。

> **转向反了怎么办**：**对调电机的两根线**最省事。不要去改 `main.h` ——
> 方向编码在 `Hardware/Motor/Motor.c` 的 `apply_output()` 里，`main.h` 只有引脚定义。

### 2.2 限位开关（KW12-3，SPDT 三脚）

**⚠️ 这一节先读第 5 节再动手。** 简版：

| KW12-3 触点 | 接到 |
|---|---|
| `COM` | **GND** |
| `NO` | **PA0**（开门限位）/ **PA1**（关门限位） |
| `NC` | **先空着**（原因见第 5 节） |

加 10 kΩ 上拉到 3V3、100 nF 到 GND（可选但推荐）。

**先用万用表找出三个脚**（不同批次脚位不一样）：

1. 手柄**不按**时，哪两个脚导通 → 那就是 `COM`–`NC`
2. 剩下那个是 `NO`
3. 按下手柄，确认 `COM`–`NC` **断开**、`COM`–`NO` **导通**

> 极性：**到位时引脚读低**（NO 闭合接地）。这已经写进 `Limit.c`（`activeLevel = 0`）。
> 若你量出来相反，**改接线或改 `activeLevel`，不要两个都不改** ——
> 那样限位会完全不起作用，而你会以为它在工作。

### 2.3 按键（KEY1–KEY4）

一端接 MCU 引脚，另一端接 **GND**。内部上拉已开。

| 按键 | 引脚 | 功能 |
|---|---|---|
| KEY1 | PB5 | 启停系统 |
| KEY2 | PB6 | AUTO / MANUAL |
| KEY3 | PB7 | 手动开门 |
| KEY4 | PB8 | 手动关门；**长按 = 急停** |

### 2.4 门内外传感器（现在用按键模拟）

| 传感器 | 引脚 | 说明 |
|---|---|---|
| 门外（进门） | **PB12** | 一端接引脚，一端接 GND。按下 = 检测到人 |
| 门内（出门） | **PB13** | 同上 |

> **不是 PB0/PB1** —— 那是旧引脚，在 round 11 改掉了。
> 原因：STM32F1 的 EXTI 线按**引脚编号**共用，PB0/PB1 会和限位抢 EXTI0/EXTI1，
> 结果是**限位一个中断都没有**。现在 `main.h` 有编译期断言挡住这种重复。

### 2.5 OLED（SSD1306 / OLED12864，I2C）

| OLED | 接到 |
|---|---|
| `SCL` | **PB10** + 4.7 kΩ 上拉到 3V3 |
| `SDA` | **PB11** + 4.7 kΩ 上拉到 3V3 |
| `VCC` | 3V3（多数 0.96" 模块支持 3.3 V） |
| `GND` | GND |

### 2.6 EEPROM（AT24C32 模块）

与 OLED **共用同一条 I2C 总线**：

| EEPROM | 接到 |
|---|---|
| `SCL` | **PB10**（和 OLED 的 SCL 并联） |
| `SDA` | **PB11**（和 OLED 的 SDA 并联） |
| `VCC` / `GND` | 3V3 / GND |

> 模块的 A0/A1/A2 全部接 GND 或按模块默认，地址为 **0xA0**（7 位 0x50）。

### 2.7 蜂鸣器（有源）

| 接法 | 说明 |
|---|---|
| **模块自带驱动** | 信号脚 → **PB3**，VCC/GND 接 3V3/GND（3.3 V 型）或 5 V（看模块） |
| **裸蜂鸣器** | PB3 → **1 kΩ** → NPN 基极（S8050）；蜂鸣器接 +5 V 与集电极；**并 1N4148 续流**（阴极对 +5 V） |

**高电平响**（`BUZZER_ACTIVE_HIGH = 1`）。共 7 种节奏：开机就绪、开门、关门、到位、故障、急停。

### 2.8 状态 LED

**PB4 → 330 Ω → LED 阳极 → LED 阴极 → GND**（高电平亮）。

固件给它 7 种闪烁模式，从房间另一头就能看出门态：

| 状态 | 模式 |
|---|---|
| INIT | 快双闪（启动中） |
| IDLE | 常灭 |
| OPENING | 常亮 |
| OPEN | 慢闪（正在倒计时） |
| CLOSING / REVERSING | 快闪 |
| STOPPED | 双闪（故障或急停） |

### 2.9 串口（USB-TTL / CH340）

| USB-TTL | 接到 |
|---|---|
| `TXD` | **PA10**（MCU 的 RX） |
| `RXD` | **PA9**（MCU 的 TX） |
| `GND` | GND |

115200 8N1。**TX/RX 必须交叉** —— 这是最常见的"串口无输出"原因。

### 2.10 ST-Link（SWD）

| ST-Link | 接到 |
|---|---|
| `SWDIO` | **PA13** |
| `SWCLK` | **PA14** |
| `GND` | GND |
| `3V3` | 3V3（若由 ST-Link 供电） |

> ST-Link 上电后**不要**打开 STM32CubeProgrammer / Keil —— 它们会独占探针，
> 表现就是"找不到调试器"。

---

## 3. 电源与地

### 3.1 三个电源域

```
                     ┌──────────────────────┐
 5V 适配器 ──────────┤ 5V                   │
 (≥1 A)              │        Blue Pill     │
                     │  (STM32F103C8T6)     │
                     │   AMS1117 ──▶ 3V3    │
                     └───┬──────────┬───────┘
                         │          │
                   3V3 ──┴──▶ OLED / EEPROM / 上拉电阻 / 传感器按键
                         │
                   5V ───┴──▶ L9110S VCC ──▶ 130 电机
```

| 域 | 供给 | 谁用 |
|---|---|---|
| **3.3 V** | Blue Pill 板上 AMS1117 | OLED、EEPROM、I2C 上拉、传感器/按键（其实用内部上拉也行） |
| **5 V** | 适配器（或电池盒经降压） | L9110S 与电机 |
| **VBAT** | 不用 | — |

### 3.2 ⚠️ 两条供电纪律

1. **不要用 ST-Link 的 5 V 带电机。** 那种小 dongle 的 5 V 直通 USB，
   电机一启动就把 MCU 一起拉跌，你会看到莫名其妙的复位。电机必须独立 5 V。
2. **地单点汇聚，电机电流不要流经 MCU 地。** 正确做法：电源负极 → 一条粗线到
   L9110S 的 GND，**另一条**到 Blue Pill 的 GND，两条只在电源负极处汇合。
   若让电机电流穿过 MCU 的地线，I2C 会出错、ADC 会飘、还可能复位。

---

## 4. 总图（ASCII）

```
                    ┌─────────────────────────────┐
   USB-TTL ─RX──────┤PA9  (TX)                    │
           ─TX──────┤PA10 (RX)                    │
                    │                             │
   ST-Link ─SWDIO───┤PA13                         │
           ─SWCLK───┤PA14                         │
                    │                             │
   蜂鸣器 ◀──────────┤PB3                          │
   LED    ◀─330Ω────┤PB4                          │
                    │                             │
   KEY1 ──┴GND──────┤PB5                          │
   KEY2 ──┴GND──────┤PB6      STM32F103C8T6       │
   KEY3 ──┴GND──────┤PB7                          │
   KEY4 ──┴GND──────┤PB8   (Blue Pill)            │
                    │                             │
   OLED  SCL ──┬────┤PB10                         │
   EEPROM SCL ─┘    │                             │
   OLED  SDA ──┬────┤PB11                         │
   EEPROM SDA ─┘    │                             │
        ↑ 各 4.7k ▲ │                             │
        到 3V3       │                             │
                    │                             │
   门外传感器 ─┴GND─┤PB12                         │
   门内传感器 ─┴GND─┤PB13                         │
                    │                             │
   L9110S A-1A ◀────┤PA4   (SW-PWM)               │
   L9110S A-1B ◀────┤PA5   (方向)                 │
                    │                             │
   开门限位 NO ──────┤PA0   ┐ 低=到位              │
   关门限位 NO ──────┤PA1   ┘                     │
                    │                             │
              3V3 ──┤3V3                          │
              5V  ──┤5V     ← 见第 5 节！          │
              GND ──┤GND                          │
                    └─────────────────────────────┘

   L9110S: A-1A/A-1B 接 PA4/PA5；MOTOR A 接 130 电机（并 SS34）
           VCC 接 5V 的方式 —— 见第 5 节，不要照旧文档串 NC
```

---

## 5. ⚠️ 限位"硬件断电"层的死锁问题（接线前必读）

### 5.1 旧文档写的接法

`docs/立创EDA原理图设计说明.md` 与 `docs/上电调试步骤.md` 都写着：

```
+5V ──[ 开门限位 COM-NC ]──[ 关门限位 COM-NC ]──▶ VM(L9110S)
```

意图是：限位一到位就用**机械触点切断电机供电**，即使 MCU 死机也停得住
（fail-safe，失效方向导向"断开"）。这个意图是对的。

### 5.2 但它和软件模型**互斥**

门在**静止关闭**时，关门限位是被门体**压住**的 → 它的 `NC` 是**断开**的
→ 两个 NC 串联 → `VM` 没有电 → **电机根本转不起来** → **门永远开不了。**

而且软件**要求**这个开关在静止时被压住：`Door_Init()` 靠 `Limit_IsClosed()`
判断"门是关着的"并进入 `IDLE`。如果门静止时开关没被压住，固件会认为门位置未知。

> **两个各自都对的子系统，不能同时成立** —— 这正是本项目反复出现的那类问题，
> 只不过这次在硬件层。而且它和 round 12 修掉的软件 bug 是**同一个形状**：
> **保护动作发生在"门需要动"的状态里**（那个是释放边沿切电机，这个是停位切断供电）。

### 5.3 一分钟确认（万用表，不用通电）

门放在静止关闭位置，量**关门限位**的 `COM`–`NC`：

- **断开** → 死锁成立，**不要**按旧文档串这个回路
- 导通 → 说明你的开关是"碰一下就离开"的安装方式；但那样固件又会认为门没关到位
  （`Limit_IsClosed()` 为假），得回到 §5.4 重新选一条路

### 5.4 两条出路

**方案 A（推荐先做）：限位只做信号，硬件断电层**暂时留空**。**

- 限位接 `NO` → PA0/PA1（§2.2），`NC` 空着
- 停机由**软件**做：限位 EXTI 立即 `Motor_EmergencyStop()`（round 11/12 已修好）
- 代价要说清：**MCU 死机时软件层失效**，电机会一直顶到机械限位处堵转，
  L9110S 与电机会发热。桌面模型 + 短行程 + 演示场合可以接受，
  **但文档里不能再声称"硬件断电层存在"**

**方案 B（要真正的硬件层就得这样）：做"超行程"切断，而不是"停位"切断。**

- 每个方向装**两个**开关（或用一个由门体继续推动的杠杆）：
  - 第一个开关在 **90°（电气限位）** 动作 → 只做**信号**给 MCU
  - 第二个开关在 **约 95°（机械超行程）** 才断开 → 串在 `+5V → VM` 里
- 静止在 90° 时第二个开关**仍闭合** → 无死锁；只有"软件没停住、继续冲到 95°"
  才会被硬件切断 → **真正的 fail-safe，且不挡正常动作**
- 代价：再买 2 个 KW12-3，并要把杠杆几何调到 90°/95° 两个动作点

> **一句话**：串联 NC 断电只能切"**过头**"，不能切"**到位**"。
> 旧文档把它当成了后者。

---

## 6. 元件清单（接线用到的）

| 元件 | 数量 | 用途 | 必需？ |
|---|---|---|---|
| 4.7 kΩ（或 10 kΩ） | 2 | I2C SCL/SDA 上拉 | **必需**，缺了 I2C 读不到高电平 |
| 10 kΩ | 8 | 限位×2、按键×4、传感器×2 外部上拉 | 可选（内部上拉已开），推荐 |
| 100 nF | 8 | 同上，到 GND 硬件消抖 | 可选，推荐（电机噪声） |
| 330 Ω | 1 | 状态 LED 限流 | 必需（否则烧 LED） |
| 1 kΩ | 1 | 蜂鸣器 NPN 基极限流 | 仅裸蜂鸣器需要 |
| S8050 | 1 | 蜂鸣器驱动 | 仅裸蜂鸣器需要 |
| 1N4148 | 1 | 蜂鸣器续流 | 仅裸蜂鸣器需要 |
| **SS34** | 1 | **电机续流** | **必需**（否则打 L9110S） |
| KW12-3 | 2（方案 B 需 4） | 限位 | 见第 5 节 |
| 5 V 电源 ≥1 A | 1 | 电机 | **必需** |

---

## 7. 上电前检查（万用表，10 分钟）

| # | 检查 | 判定 |
|---|---|---|
| 1 | 5 V 电源空载 | 4.9–5.1 V |
| 2 | Blue Pill 3V3 | 3.2–3.4 V |
| 3 | **限位 COM/NC/NO 脚位** | 见 §2.2 —— **最容易做错的一步** |
| 4 | **静止时 `COM`–`NC` 通断** | 见 §5.3 —— 决定走方案 A 还是 B |
| 5 | 不按时 PA0/PA1 对地 | **3.3 V**（上拉）；按下 → **0 V** |
| 6 | I2C `SCL`/`SDA` 对 3V3 | 约 3.3 V（上拉在，总线空闲） |
| 7 | 电机端子二极管方向 | 阴极对 +5 V |
| 8 | 电机地与 MCU 地 | 只在电源负极一点相连，**不通** |
| 9 | 上电瞬间 | 蜂鸣器一声（就绪），LED 快双闪，串口出开机横幅 |

---

## 8. 常见错误对照

| 现象 | 首先怀疑 |
|---|---|
| 串口无输出 | PA9/PA10 **没交叉**；波特率不是 115200 |
| `I2C idle: NO (bus held low)` | **上拉电阻没接**；或 SDA/SCL 接反；或器件短路 |
| `Display: not found` | OLED 的 VCC/GND、地址 0x78、PB10/PB11 |
| `Log: UNAVAILABLE` | EEPROM 未应答 —— 门控仍工作，但不持久化 |
| `Limits: open=0 closed=0` | 限位接线错，或上拉缺失 |
| `Limits: open=0 closed=1`（门关着） | ✅ 正常 |
| `Limits: open=1 closed=1` | 两限位同时压下 → `LIMIT_CONFLICT`，物理上不可能，查短路 |
| 状态 LED 不亮 | 接在 **PC13** 了 —— 应该接 **PB4** |
| **发 `DOOR=OPEN` 门一动不动** | **限位 NC 串了电机供电（§5.2 死锁）** |
| 发 `DOOR=OPEN` 只动几毫米就停、5 s 后 `OPEN_TIMEOUT` | 限位**释放边沿**切了电机 —— round 12 已修；跑 `python tools/check-limit-safety.py` |
| 电机转但方向反 | 对调电机两根线 |
| 电机不转但 L9110S 发热 | 电机线接错，或电机电压不足 |

---

## 9. 相关文档

| 文档 | 用途 |
|---|---|
| `docs/上电调试步骤.md` | 7 步验证流程 + 台架模式 + round 11/12 修复的专项判据 |
| `docs/Proteus仿真方案.md` | 仿真接线（含 **I2C 上拉**与 OLED12864 模型） |
| `docs/立创EDA原理图设计说明.md` | 画原理图/PCB 用；**其 §4.1 的 NC 串联断电按 §5 处理** |
| `generated/netlist.txt` / `mcu-pinout.csv` | 机器生成的网表，与固件一致 |
| `tools/gen-hardware.py` | 重新生成上面两份产物并交叉校验 |
